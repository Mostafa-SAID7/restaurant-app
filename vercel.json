{
  "buildCommand": "cd frontend && npm ci && npm run build",
  "outputDirectory": "frontend/dist/restaurant-app/browser",
  "rewrites": [
    {
      "source": "/(?!.*\\.)",
      "destination": "/index.html"
    }
  ]
}
